/** Everything public is mounted under this. Versioned from the first commit: the
 *  mobile client will be built against v1 by someone who is not in this repo. */
export const API_PREFIX = '/api/v1';

export const BEARER_PREFIX = 'Bearer ';
